# W3-B -> I3 bounded integration requests

1. Consume the exact accepted W3-A checkpoint (never dirty bytes). Supply its actual
   command/entrypoint, process stop behavior, bounded timeout, exit classes and sanitized
   run/correlation evidence. Preserve RECURRING_BILLING_CALLER and
   CREATE_ELIGIBLE_RECEIVABLE_ONLY. Bind propertyai_rent_scheduler without Finance or
   Web authority widening. The W3-B slot is intentionally UNBOUND_I3 and OFF; no module
   filename or callable has been guessed. Rebuild the manifest from the exact I3 source
   commit, replacing this slot only after accepted integration evidence.
2. Review the additive W3-B Web composition against shared I2 composition and bind any
   shared launch registration/entrypoint only under I3 ownership. W3-B changed no shared
   router, domain, DB migration, dependency lock or W3-A code.
3. External activation remains a later approved target/secret/provider/IdP/backup and
   durability decision. Neither W3-A acceptance nor local operational PASS authorizes
   a real scheduler, Production data, public endpoint, external service or paid resource.

## I3 resolution

The integration worktree consumed both frozen sibling checkpoints from their common I2
base before making integration-owned edits. The W3-A six-path set remains byte-identical.
I3 binds the exact accepted callable
`propertyai_core.rent_recurring_billing.run_recurring_billing_once` version 1 through a
single-run runtime process. Its manifest binds the common base, W3-A checkpoint and W3-B
checkpoint; scheduler default remains OFF and there is no automatic activation path.
The runtime distinguishes continuation, contract failures, unknown effects, config errors
and runtime errors without changing W3-A billing or Finance semantics. External target,
IdP/secrets, backup durability/provider, scheduler activation and Production deployment
remain outside I3.
